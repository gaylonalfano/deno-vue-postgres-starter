// TODO Not sure how to refactor this. Adding to server.ts for now

/* import { log } from "../deps.ts"; */

/* export async loggingMiddleware(ctx, next) => { */
/*   await next(); */
/*   // We now have access to response.headers from downstream middleware */
/*   const time = ctx.response.headers.get("X-Response-Time"); */
/*   log.info(`${ctx.request.method} ${ctx.request.url} ${time}`); */
/* } */
